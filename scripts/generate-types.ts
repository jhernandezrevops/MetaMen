/**
 * METAMEN100 - Generate Types Script
 * Genera tipos TypeScript desde Supabase
 *
 * Uso: pnpm db:generate
 *
 * Nota: Este script es un wrapper para documentación.
 * El comando real está en package.json: db:generate
 */

console.info('📦 Generating Supabase types...');
console.info('');
console.info('Run: pnpm db:generate');
console.info('');
console.info('This will generate types to: src/types/database.types.ts');
console.info('');
console.info('Requirements:');
console.info('  - SUPABASE_PROJECT_ID environment variable');
console.info('  - Supabase CLI installed (npx supabase)');
console.info('  - Logged in to Supabase CLI');
